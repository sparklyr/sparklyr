pipeline {
    agent any
    stages {
        stage('Set up Databricks CLI') {
            steps {
                echo 'Hello world!'
                pip install databricks-cli
            }
        }
    }
}
