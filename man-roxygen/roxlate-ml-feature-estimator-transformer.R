#' @details In the case where \code{x} is a \code{tbl_spark}, the estimator fits against \code{x}
#'   to obtain a transformer, which is then immediately used to transform \code{x}, returning a \code{tbl_spark}.

